import "rc-slider/assets/index.css";

import RCSlider from "rc-slider";

export const Slider = RCSlider.createSliderWithTooltip(RCSlider);
export const Range = RCSlider.createSliderWithTooltip(RCSlider.Range);
